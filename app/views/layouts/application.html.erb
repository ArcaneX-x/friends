<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for?(:title) ? yield(:title) : 'Friends Event' %></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <%= favicon_link_tag asset_path('favicon.png'), rel: "icon" %>
  <%= favicon_link_tag asset_path('apple-touch-icon.png'), rel: "apple-touch-icon" %>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application" %>
  <!-- Google Fonts -->
  <%= stylesheet_link_tag "https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,700,700i|Raleway:300,400,500,700,800" %>
</head>
<body>
<% if current_page?(root_path) %>
  <%= render 'layouts/home_header' %>
<% else %>
  <%= render 'layouts/header' %>
<% end %>
<%= yield %>
<%= render 'layouts/footer' %>
</body>
</html>
